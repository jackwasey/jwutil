#' @param startDate This should be a single YYYY-MM-DD string. There are no absolute requirements about the format, but it is intended to be the same format as used in a SQL query.
#' @param endDate see start date
